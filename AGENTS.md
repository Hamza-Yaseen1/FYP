<!-- BEGIN:nextjs-agent-rules -->

# This is NOT the Next.js you know

This version has breaking changes — APIs, conventions, and file structure may all differ from your training data. Read the relevant guide in `node_modules/next/dist/docs/` (resolved from this file's directory; in monorepos the `next` package may not be visible from the repo root) before writing any code. Heed deprecation notices.

This block is written and re-added by `next dev` — verify at `node_modules/next/dist/server/lib/generate-agent-files.js`. Removing it from a diff only re-creates the uncommitted change; committing it with your work keeps the tree clean.

<!-- END:nextjs-agent-rules -->

# my-app Development Guidelines

Auto-generated from all feature plans. Last updated: 2026-08-28

## Active Technologies
- Python 3.13 + pytests, `unicodedata` (stdlib) for script detection (015-ai-orchestrator)
- MongoDB `messages` collection — `ai_analysis` gains a `routing` subdocument (015-ai-orchestrator)
- Python 3.13 + pytests; `re` + `datetime.timedelta` (stdlib) for (016-agent-memory)
- MongoDB `messages` collection — three additive identity fields (016-agent-memory)
- Python 3.13 (backend), TypeScript 5.x (frontend) + FastAPI, Motor, Pydantic, `httpx` (NEW runtime dep; already present for tests), `cryptography` (already used), PyJWT (already used) (017-gmail-integration)
- MongoDB — `connections` (extended) and `messages` (additive `subject`) (017-gmail-integration)
- TypeScript 5.x (React/Next.js App Router, `"use client"`); Python 3.13 backend (unchanged) + Next.js, Tailwind CSS v4, shadcn/ui (base-nova), lucide-react, `@/lib/api` (all already in use) (018-cross-platform-dashboard)
- MongoDB `messages` collection — **read-only reuse**, no schema or migration changes (018-cross-platform-dashboard)
- Python 3.13 (FastAPI backend), TypeScript 5.x (Next.js 16.3.1, React 19) + motor (MongoDB aggregation), recharts (NEW — charts), existing shadcn/ui + Tailwind v4 + lucide-react (019-analytics)
- MongoDB `messages` and `tasks` collections, **read-only reuse** — no schema or migration changes (019-analytics)
- Python 3.13 (backend only — zero frontend changes) + existing motor/pymongo/pytest/httpx; **no new dependencies** (stdlib `hmac`, `hashlib` already in use) (020-security-testing)
- MongoDB — `messages` (index change: composite `{user_id, external_message_id}` dedupe index), `tasks`/`connections`/`users` (read-only verification) (020-security-testing)

- Python 3.13 + FastAPI, uvicorn, motor, pydantic, python-dotenv (014-whatsapp-webhook)
- MongoDB (`messages`, `connections`, `users` collections) - add a dedup index. Use documentation-driven design for day-by-day features (014-whatsapp-webhook)

## Recent Changes

- 017-gmail-integration: Added Python 3.13 + FastAPI, Motor, Pydantic, httpx (runtime), PyJWT; MongoDB connections/messages extensions for Gmail OAuth receive
- 014-whatsapp-webhook: Added Python 3.13 + FastAPI, uvicorn, pydantic, motor, python-dotenv
- 015-ai-orchestrator: Added Python 3.13 + rule-based AI orchestrator (routing, single-call gating, trivial-skip)

<!-- MANUAL ADDITIONS START -->
- 015-ai-orchestrator man: The single AI entry point is now `services/ai/orchestrate.py::process_message` (decide_routing -> analyze_message with internal gating -> attach `ai_analysis.routing`). Trivial messages short-circuit to deterministic defaults and no-content/media to a `skipped` stub — zero LLM calls on both. `routing` is attached on EVERY path (`llm_call_used=true` even on `pending` failures; a `_analyze_with_fallback` wrapper guards `analyze_message`/`get_provider` crashes). Routes/services must NEVER call `analyze_message` directly. Tests: never await motor under `asyncio.run` (use the TestClient loop or a fake collection). Debug/verification scripts can call `analyze_message` directly. Backend uses MongoDB Atlas (`MONGO_URI=mongodb+srv://` in `backend/.env`); auth is cookie-based (register/login set a session cookie — `httpx.AsyncClient` persists it). Standalone scripts `test_attention_pipeline.py`/`test_priority_integration.py` are era-stale (live-server auth-gated; shared-Atlas owner resolution) — kept as historical artifacts. `POST /messages` analyzes synchronously; `/webhooks/simulate` uses a background task (`ai_analysis: null` on the response). Full suite ~125 tests, needs 300s shell timeout.
- 016-agent-memory man: Deterministic link lives in `services/threads.py::resolve_and_stamp` (stdlib `re` + `timedelta`; same `user_id` + `source` + `normalize_sender` + ≤`LINK_WINDOW_MINUTES`; `threadId` = anchor `_id`, only minted after a 2nd in-window message; every query user-scoped). Identity stored at BOTH ingest entry points (`services/webhook_ingest.py::ingest_message`, `routes/messages.py::create_message`): `messageId` always set server-side pre-insert, `threadId`/`conversationId` only when linked. `services/ai/context.py` does fetch/build/validate/apply: `fetch_thread_context` (≤5 same-user messages, excludes current, `handle = _id[-7:]`), `build_context_block(messages, current_message_id=None)` renders the CONTEXT block (with a "(THIS message being analyzed)" line when the current message id is given — it is a valid `context_updates` source), `validate_context_updates(candidates, thread_messages, current_message_id, current_content=None)` accepts ONLY verbatim values from a provable source (earlier thread message OR the current message), `apply_context_updates` is `$push`-only on `{_id, user_id}`. Analyzer calls `provider.analyze(msg, context=None, current_message_id=None)` (single call, context rides inside; `context=None` prompt is byte-identical to Day 24). Orchestrator pops `context_updates` before returning the current analysis and never lets enrichment block ingestion. Do NOT call `analyze_message` from routes/services. Frontend untouched (Dashboard reads stored `ai_analysis` only). Suite 155, 300s timeout.
- 017-gmail-integration man: Gmail connect uses official Google OAuth 2.0 via direct REST (httpx, no SDK). OAuth `state` = HS256 JWT (`JWT_SECRET`, `purpose="gmail_oauth"`, `sub=user_id`, 10-min `exp`) — `services/gmail.py::verify_state` returns None on any mismatch (CSRF + identity). `GET /connections/gmail/auth-url` (auth-gated) + `GET /connections/gmail/callback` (never auth-gated — reads cookies; state binds to the user; on success re-encrypts BOTH tokens via `utils/encryption.encrypt` and `upsert_gmail_connection`). Scope is `gmail.readonly` ONLY; refresh_token REQUIRED (missing ⇒ error redirect). Receive path: asyncio poller started in `main.py` lifespan when `GOOGLE_CLIENT_ID` set (NOT on `communication_ai_test` — avoids real Google calls in tests), interval `GMAIL_POLL_INTERVAL_SECONDS` (30s), watermark `last_fetched_at` via `q=after:<epoch>`; `normalize_email` uses stdlib `email` MIME parse (text/plain, HTML fallback, attachments skipped); `_clean_sender` strips the email parser's added quotes. Ingest via `ingest_message(..., subject=...)` (subject additive; absent for WhatsApp). `ingest_message` spawns `asyncio.create_task(_analyze_and_store)` when `background_tasks is None` (tracked in module set). Disconnect `DELETE /connections/{id}` best-effort revokes Google token first, then user-scoped delete. Poller failure (token unrefreshable) → `set_connection_error` → frontend `status === "error"` Reconnect CTA. Message responses now include optional `subject`; MessageList renders `Re: <subject>` when present. `GET message format=raw` returns base64url MIME in the TOP-LEVEL `raw` field (`payload.raw` exists only in test doubles) — `normalize_email` must read `raw.get("raw") or raw["payload"].get("raw")`, else real emails parse with empty sender/content/subject. Groq provider MUST call with `reasoning_effort="none"` + `response_format={"type":"json_object"}` + `max_tokens≤~700`: default Qwen 3.6 27B reasoning burns the whole output budget and trips the free-tier OTPM 429 guard (a failed analysis surfaces as `Normal 0%` + Review/Verify badges + no summary/recommended action). TEST RULES: never run real motor under `asyncio.run` (use fake collections like `_FakeMessages`, or the TestClient loop) — `asyncio.get_event_loop()` fresh-loop fails with "Future attached to a different loop" when motor client is bound to the TestClient portal; monkeypatch `httpx.AsyncClient` for ALL Google calls. Full suite 197 tests, needs 600s shell timeout.
- 019-analytics man: `GET /analytics?period=week|day|month` (default `week`, auth-gated, invalid → 400 exact detail string) reads messages+tasks READ-ONLY. `services/analytics.py::build_analytics_response` aggregates per-request from live data, never storing. RULES: `user_id` is ALWAYS the first `$match` in every pipeline (no cross-user aggregation); priority mapping = `urgent/important/normal/low` recognized, ANYTHING else (unknown/missing/`pending`) → `pending` bucket (keep `pending` visible — un-analyzed shown separately, not dropped); tasks period-scoped via parent message `received_at` resolved from `source_message_id` (user-scoped `$in`), falling back to task `created_at`; trends granularity = hourly for `day` (`YYYY-MM-DDThh:00:00Z`), daily for `week`/`month` (`YYYY-MM-DD`) via `$dateToString` in `$group` then Python zero-fill of the FULL `[from,to]` window (floored edges so no boundary message drops; no skipped buckets). Response shape: `{period, from, to, total, by_priority, by_source, tasks:{total,completed}, trends:[{date,count}]}`. Dates are UTC-aware `datetime` (FastAPI serializes ISO). Frontend: recharts ^3.10 is THE chart lib; always color via theme CSS vars (`--signal` purple, `--ember`, `--cool`, `--chart-1..5` — NEVER hex) so light/dark stay legible; recharts `ResponsiveContainer` needs a `ResizeObserver` stub in vitest; screenshot-able sections wrap in CSS-var `contentStyle` tooltips. Analytics page (`app/(dashboard)/analytics/`) is server-independent: `analyticsFetch(period)` re-fetches on PeriodToggle with no reload; `TrendChart` tick formatter shortens labels (`.slice(11,16)` for hourly / `.slice(5)` for daily). Tests: backend `tests/test_analytics.py` uses `_FakeAnalyticsMessages` (supports `$match`/`$count`/`$group` incl. `$dateToString` `_id` and `find`+`$in`) + `_FrozenDatetime` to pin windows; operational suite = `python -m pytest tests/` (212 passing) — era-stale standalone `test_*.py` root scripts (`test_priority_integration.py` etc.) STILL fail on `async def` collection + shared-Atlas and are disabled/kept as artifacts, DON'T chase them.
- 020-security-testing man: Two verified, two implemented. (1) Isolation: cross-user DELETE/GET → 404, analytics blocked, list excludes — all enforced by `$match` with `user_id`. (2) Webhook signature: SHA-256 HMAC, empty/missing/malformed → 403; verification challenge handshake; key rotation safe. (3) AI reliability: pending analyses visible; `retry_pending_analyses()` re-enters `process_message` (never `analyze_message`), user-scoped `$set`, idempotent, guarded per-message. Wired into `main.py` lifespan (`RETRY_INTERVAL_SECONDS` default 60s, skipped on test DB). (4) Dedupe: composite `{user_id, external_message_id}` replaces legacy global index (dropped at startup); `DuplicateKeyError` recovery scoped to `{user_id, external_message_id}`; cross-user same-key independent. New: `services/retry_pending.py`, 4 test files (17 new tests). Suite 229, 600s timeout.
- 020-security-testing: Two verified, two implemented — composite `{user_id, external_message_id}` dedupe index, `DuplicateKeyError` scoped recovery, `retry_pending_analyses()` sweep wired in lifespan; 17 new tests (229 total)

## Recent Changes

- 017-gmail-integration: Added Python 3.13 + FastAPI, Motor, Pydantic, httpx (runtime), PyJWT; MongoDB connections/messages extensions for Gmail OAuth receive
- 014-whatsapp-webhook: Added Python 3.13 + FastAPI, uvicorn, pydantic, motor, python-dotenv
- 015-ai-orchestrator: Added Python 3.13 + rule-based AI orchestrator (routing, single-call gating, trivial-skip)
